# 作業ルール

## テスト実行方針

- 通常の検証では、設計上必ず実時間で200ms以上かかるテストを実行しない。実時間の待機・連続入力・ブラウザー起動等が必須のテストや手動検証も対象とする。
- これらは、ユーザーが「全てのテストを実行」または「時間のかかるテストを実行」と明示した場合だけ実行する。「テストして」「検証して」だけでは実行しない。
- 判定はテスト設計の必須実時間による。テスト一式の合計時間や一時的な実行環境の遅さでは判定せず、200msでテストを打ち切るタイムアウトも設定しない。
- 偽時計で500msや2秒を進めるだけで実際には待たない単体テストは、通常の検証に含める。時間依存の仕様は原則として偽時計で検証する。
- 遅い自動テストは `tests/slow/` 配下、または `*.slow.test.mjs` に分離する。遅いテストを通常の `*.test.mjs` に混在させず、モジュールのトップレベルでも重い検証を実行しない。
- 通常は `npm test`、明示要求時だけ `npm run test:all` または `npm run test:slow` を使う。対象を事前確認する場合はそれぞれに `-- --list` を付ける。
- 型チェック・ビルドはテストとは区別して必要に応じて実行できる。未実施の遅い検証は、実施済みとして報告しない。
