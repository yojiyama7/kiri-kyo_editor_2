# 下線作成後のカーソルと履歴

2026-09-03、検証用 `127.0.0.1:5191` で実施。
表示状態JSONの文書・カーソルを作成直前、作成直後、Undo後、Redo後で比較した。

- [x] 初期サンプルの先頭で `v` → `l` → `Enter`。複合下線が1つ増え、新しい下線のSlot IDへ移動する（X=1、Y=1）。
- [x] `vv` で先頭と「the student」の下線を個別選択し、未選択の「gave」へ戻って `Enter`。非連続下線を作成し、隙間ではなく新しい下線自身のスロットへ移動する。
- [x] 初期サンプルの「the student」の基礎下線へ移動し、`k` → `v` → `l` → `Enter`。新しい基礎下線自身のスロットへ移動する（X=3、Y=0）。
- [x] 上記3ケースとも、1回の `u` で作成直前の文書・カーソルを復元する。
- [x] 上記3ケースとも、1回の `Ctrl+r` で作成直後の文書・カーソルを復元する（新しい下線のIDも一致）。
- [x] ブラウザのエラーログなし。

既存の自動テスト173件、`npm run check`、`npm run build` も成功。
