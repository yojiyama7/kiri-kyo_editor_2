# 複数組のブラウザ確認

2026-09-02、検証用 `127.0.0.1:5174` と初回起動用 `127.0.0.1:5189` で実施。

- [x] 3組を表示し、追加した空の組で英文入力が開始される。英文・訳文を個別に編集できる。
- [x] `Ctrl+n/p` で前後の組へ移動し、移動先のカーソルがX=0になる。選択中の移動で選択とモードを解除する。
- [x] 英文入力中の `Ctrl+p` は組を移動せず、入力モードを維持する。
- [x] `Alt+j` で組が入れ替わり、組IDとX=1のカーソルを維持する。Undo / Redoで順序を復元できる。
- [x] 2組目で標識、T化、下線、矢印を編集しても1・3組目のデータは変化しない。
- [x] スロットをクリックしてから `r` → 移動 → `Enter` で矢印を確定できる。フォーカスされたスロットボタンのクリック処理と競合しない。
- [x] 非アクティブの組にカーソルの強調が残らない。
- [x] 別の組からUndoすると対象の組へ戻る。組の削除をUndoすると組ID・全文・構造・標識・T化・矢印を復元する。
- [x] 全組を削除しても空の1組を残す。連続した3回の削除をUndoすると元の文書を完全に復元する。
- [x] 基礎下線の内部編集を開いたまま別の組へ移動し、その別の組の編集をUndoしても、元の組の構造が変化しない。
- [x] 英文入力中に別の組の訳文編集をクリックすると英文を確定する。さらに別の組をクリックして訳文を確定し、Undoで訳文→英文の順に戻せる。
- [x] 再読み込みで全組の順序・ID・構造・訳文を復元する（JSONのキー順は除いて比較）。
- [x] 390px幅で組ごとに2行・2行・1行へ折り返す。幅変更で保存文書は変化せず、スクリーンショットで下線・矢印・接続点を確認した。
- [x] 未保存の初回起動直後から、表示文書と一致するv6が自動保存される。
- [x] ブラウザのエラーログなし。検証後に幅指定を解除し、検証タブと開発サーバーを終了した。

自動テストは全158件。`entryDocument.test.mjs` はv5移行、v6読み込み、組間参照の拒否、組操作と文書全体の履歴を検証する。`entryShortcuts.test.mjs` は修飾キー、macOS Option、IME、テキスト入力中、既存キーとの非競合を検証する。`npm run check` と `npm run build` も成功。

## 訳文入力中のキー操作（2026-09-03）

検証用 `127.0.0.1:5191` の2組で実施。

- [x] 訳文入力中の `Tab` / `Shift+Tab` でフォーカス・選択位置・入力内容・入力モードが変わらない。
- [x] 訳文入力中の `Ctrl+p/n` で前後の組へ移動し、訳文を確定して移動先のNormalモード・X=0に戻る。
- [x] 先頭での `Ctrl+p`、末尾での `Ctrl+n` は訳文入力とフォーカスを維持する。
- [x] 移動時に確定した訳文をUndo / Redoできる。変更せずに文を移動しても履歴を追加しない。
- [x] 英文入力中の `Ctrl+p` は組を移動せず、入力モードを維持する。
- [x] 再読み込み後に両方の訳文を復元する。ブラウザの警告・エラーログなし。

自動テスト全203件、`npm run check`、`npm run build` が成功。IME変換中と追加の修飾キーによる文移動の抑止、入力中の `Alt+j/k` の抑止はショートカットの自動テストで確認した。
