# 矢印のブラウザー確認

## 終点選択中の基礎下線への出入り

2026-09-03、独立した検証用オリジン `http://127.0.0.1:5175/` で確認。

- [x] Sheをaに変更し、`r → l → l → k → l → Enter` で基礎下線内のstudentへ接続。始点IDを保持し、内部の単語IDを終点として保存する。
- [x] 既存矢印を `r → l → l → k → Enter` でtheへ付け替える。矢印は1本のまま。
- [x] ARROWモードの `k` と `↑` で内部へ入る。内部移動・上端での空振りでは開いた状態を維持する。
- [x] ARROWモードの `h`・`l`・`j`・`0`・`$`・クリックで外へ出ると、O1が基礎下線へ戻る。矢印の始点・終点IDは変わらない。
- [x] 内部でEscを押すと既存矢印と開いた下線を維持し、Normalでその後退出すると基礎下線へ戻る。
- [x] 終点選択中の `t`・`d`・`x`・`v`・標識入力では文書とモードを変更しない。
- [x] T・D分割した基礎下線上で終点選択中に `k` / `↑` を押しても、カーソルと文書は変わらない。
- [x] 退出による復帰を `u` で取り消し、`Ctrl+r` で再適用できる。接続先は維持する。
- [x] 表示状態と保存状態の文書が一致し、再読込後もtheへの矢印と基礎下線の表示を維持する。

矢印を含む文書での進入・各退出経路・接続と付け替え・履歴・再読込・T/D例外・非連続下線の隙間は `arrows.test.mjs` に回帰テストを追加。全189件、型チェック、ビルドが成功。

2026-09-02、ローカルViteサーバーの独立したポートで実施。通常幅と390px幅で確認した。
純粋関数の自動テストは `arrows.test.mjs`、以下は実際の画面操作による確認記録。

- [x] 標識をaに変更 → r → l → Enterで矢印を作成し、画面とデバッグJSONを確認。
- [x] uで削除、Ctrl+rで復元。標識の編集とは別の履歴になっている。
- [x] 既存矢印の始点でr → Enterは自己接続を拒否し、既存矢印を保持。
- [x] 終点選択中のt・x・vでは構造・標識・選択モードを変更しない。
- [x] Escによる取消は既存矢印を保持。再びr → 別の終点 → Enterは追加でなく付け替え。
- [x] s → a → dの入力途中は矢印を保持し、sadの確定でも保持。
- [x] 始点のxは標識と矢印を削除し、1回のuで両方を復元。
- [x] Shift+rは標識を残して矢印だけ削除し、uで復元。
- [x] tは出る矢印を左スロットへ移し、Xは消失する左右に接続した矢印を削除。uでTと矢印を復元。
- [x] ページ再読込後もv5の始点・終点IDとT化を保持。
- [x] 390px幅で行をまたぐ矢印を作成。横線の両継続点が同色で、空の終点への矢じりは上辺に接続。
- [x] 別の始点から同じ終点へ接続すると、矢印データは2件、配置上の横線は1束、終点の縦線・矢じりは1本。
- [x] 通常幅へ戻すと1行に戻り、論理配置JSONは幅変更前と一致。

保存形式v5以外の拒否、開閉端点・混合配置順・連鎖削除の詳細は自動テストで検証する。

## 衝突判定・整数再採番の修正後の再検証

2026-09-02、`localhost:5174` の検証用オリジンで実施。既存の通常利用ポートの保存データは変更していない。

- [x] 初期例のO1 `[2,4)` とO2 `[4,7)` がともにY=0、親がY=1になる。
- [x] SheをT化すると左右がX=0・1、gaveがX=2になる。左右移動で両方のTスロット、gave、O1、O2を順に選べる。
- [x] 折り返されたO2の最後の表示区間をクリックすると、再採番後のX=7でO2が選択される。
- [x] 3単語のO2もT化でき、最小区画数が8から9へ増える。左右は整数X=5・7で選択できる。
- [x] O2のT化解除で最小区画数が8へ戻り、カーソルは親O2を維持する。Undo／Ctrl+rでT字とカーソル、接続矢印が復元・再削除される。
- [x] T字の左スロットから空の右スロットへの矢印を作成し、論理中点による区間 `(0.5,8]` と上辺への矢じりを確認。
- [x] 390px幅では矢印が2行に分かれ同色の接続点でつながる。1100px幅では1行になり、論理配置JSONは完全に一致する。
- [x] 標識付きO2への矢印は `(0.5,6.5]` となり、終点の下辺に接続する。
- [x] 2つの始点からO2への矢印が、1束の横線と1本の終点垂線を共有する。
- [x] v5再読込で文書と論理配置を復元する（JSONのオブジェクトキー順を除いて一致）。
- [x] T字の右スロットからgaveまでの範囲選択が、整数の始点X=1・カーソルX=2で正しいSlot IDを選ぶ。
- [x] 検証用の幅指定を解除し、タブとサーバーを終了する。

整数区間・中点・点区間の回帰テストは `slotGeometry.test.mjs` に追加。自動テストは全126件。

## 入出力が重なるスロットの描画補正

2026-09-02、`localhost:5174` の検証用オリジンで確認。

- [x] She → gave → O2 の接続で、gaveの終点X=97.2890625に対して出る始点X=103.2890625となり、右へ6px離れる。横線の左端も同じXまで短くなる。
- [x] gaveの行き先を左へ変更すると、出る始点は終点より左へ6px移動し、横線の右端もその位置に一致する。
- [x] スクリーンショットで垂線と横線の接続を確認。検証の編集はUndoで戻し、タブとサーバーを終了。

左右・共有横線の中間始点・折り返し・狭いT字・同位置の接続・削除後の復元・論理配置不変の8件を `arrowRender.test.mjs` に追加。自動テストは全139件。

## 横線をスロット中央の高さへ変更

2026-09-02、`localhost:5174` の検証用オリジンで、T字を含む2始点からO2への共有矢印を確認。

- [x] 論理Y=2の横線はSVG内の90px。SVG原点はY=0スロット上辺と一致し、Y=1スロット中央より38px下に位置する。
- [x] 始点・標識付き終点の接続位置は28pxのまま、垂線は90pxまで延びる。矢じりは終点スロット側に残る。
- [x] 390px幅で2行へ折り返すと、両行の横線・同色接続点は90px。SVG高さ104pxに半径3.5pxの接続点が収まり、切れない。
- [x] 幅変更前後の論理配置JSONは完全一致。通常幅と折り返し幅のスクリーンショットで接続を確認。
- [x] 保存データを変更せず、幅指定を解除して検証用タブを終了。

Y=1の52px・Y=2の90px、同じ段のスロット中央、空／標識付き終点の垂線、T字と共有横線の幅変更を自動テストに追加。全143件、`npm run check`、`npm run build` が成功。
