# エージェント向け案内

このリポジトリで作業するときは、最初に `llm/README.md` を読むこと。

`llm/README.md` の一覧から、現在の作業に関係する指示・記憶ファイルを少なくとも1つ選び、内容を最後まで読んでから作業すること。複数の領域にまたがる場合は、関係するファイルをすべて読むこと。

作業中に、将来の作業でも役立つ仕様、設計判断、制約、失敗しやすい点を発見した場合は、`llm/README.md` の更新方針に従って適切なファイルへ追記すること。一時的な進捗、推測、すぐ古くなる情報、秘密情報は保存しないこと。

プランに基づく作業をすべて遂行し終えたら、その作業に属する変更だけをGitコミットしてからユーザーへターンを返すこと。ユーザーによる既存の変更や、今回の作業と無関係な変更をコミットに含めないこと。コミットに失敗した場合は、完了したものとして扱わず、原因をユーザーへ報告すること。

`AGENTS.md` の作業ルールは常に優先して守ること。
