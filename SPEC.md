# Kiri-kyo Editor MVP 仕様

## 今回実装する範囲

- Svelte 5 + TypeScript の1画面アプリ
- 英文・構造図・訳文を1組として、複数の組を縦に表示する。構造と選択は組ごとに独立する
- 英文を空白でトークンに分割
- 各トークンの表示幅は、中身の単語の実幅に最小限の余白を加えた幅とする
- トークンの内容の文字列は、トークン幅の中央に表示する
- トークン列は編集領域の幅に合わせてトークン間で折り返す。幅の変更時も再配置し、単独で領域幅を超えるトークンのみ文字を折り返す
- 行をまたぐ下線は行ごとに分割し、前の行の末端と次の区間の先端を同色の丸で結ぶ。非連続選択・入れ子の下線にも適用する
- 折り返しは表示にのみ反映し、Slot の参照、論理 X/Y、選択・移動・保存の規則は維持する
- カーソルとVisual選択はトークン文字列ではなく、その直下の対応スロットを対象とする
- `h` / `l` でスロット移動。T化された左右も個別に移動する
- `j` / `k` で現在のトークン列を保ったまま、実際の配置段にある単語・基礎下線・複合下線のスロット間を上下移動
- `0` / `$` で現在のネスト深度にある先頭・末尾のスロットへ移動
- `v` で連続範囲の選択を開始し、`Enter` で下線を作成
- 基礎スロットだけでなく下線の Group Slot からも選択を開始できる。選択したスロット自体を強調表示し、新しい下線から直接参照する
- 選択範囲は両端のスロットを含み、間の各列では両端の最大 Y 以下の最も高いスロットを選ぶ。選択済みスロットが含む基礎スロットは重ねて追加しない
- `X` で現在位置を含む最新の下線を削除
- `i` で英文入力、`Tab` で訳文入力、`Esc` でNormalへ戻る
- 訳文入力中は `Enter` でも改行せず確定してNormalへ戻る。IME変換中の確定キーでは入力モードを維持する
- 訳文入力中の `Tab` / `Shift+Tab` は何もせず、フォーカス・入力内容・入力モードを維持する
- `localStorage` への自動保存

## 複数組の操作

- 各組に番号と「下に追加」「削除」「上へ」「下へ」のボタンを表示する。編集中の組を枠で示し、カーソル・選択の強調はその組だけに表示する
- 「下に追加」は直後へ空の組を挿入し、英文入力を開始する。最後の1組を削除した場合は新しい空の1組を残す
- `Ctrl+n` で次の組、`Ctrl+p` で前の組へ移動する。移動先は一番左の有効スロットを選択し、空の組も編集対象にできる
- `Alt+j` で次の組、`Alt+k` で前の組と入れ替える。組のID・内容とその組内のカーソルを維持する。macOSのOptionキーも同じ操作とする
- 最初・最後の組を越える操作は無変更。組間キーはキーリピートを無視し、処理対象の場合はブラウザの既定動作を抑止する
- 英文入力中とIME変換中は組間キーを処理しない。訳文入力中は `Ctrl+n/p` のみ処理し、訳文を確定して移動先のNormalモードへ戻る。移動先がない場合は訳文入力を維持し、ブラウザの既定動作を抑止する。入力中の `Alt+j/k` は処理しない。それ以外では標識入力を確定し、範囲選択・個別選択・矢印の終点選択を解除して移動・入れ替えする
- クリックでも組を切り替える。元の組の入力を確定し、スロットをクリックした場合はその位置を選択する。編集中の組以外へキーボード操作を配信しない
- 組間移動では構造データを変えず、Undo履歴を追加しない。非アクティブの組は内部編集時の構造も保持し、別の組のUndoによって再分類しない

### 複数英文の一括追加

- 各組の「英文を一括追加」から専用ダイアログを開く。現在の入力は通常どおり確定し、一括追加とは別の履歴にする。既存の単一組の英文編集欄は変更しない
- 英文のみを1行1組として入力する。LF・CRLF・CRを改行として扱い、各行の前後空白を除去し、行内の連続空白は単一スペースへ正規化する。空行は無視し、同じ英文の重複は保持する
- 入力欄へフォーカスし、有効な追加件数を表示する。0件の場合は追加ボタンを無効にする。Enterは改行、Tab / Shift+Tabは入力欄と有効なボタン間を循環する。一括入力中のキーは背後のエディタへ配信しない
- 「追加」で、開いた組の直後へ入力順に新規の組を挿入する。各組のID・トークン・スロットは新規生成し、訳文・下線・標識・T化・矢印は空とする。既存の組（空の組も含む）は置換しない
- 追加後はダイアログを閉じ、最初に追加した組の先頭スロットをNormalで選択し、スクロールする。一括追加全体を1回のUndo / Redoとし、Undoは追加前の組とカーソル、Redoは同じID・順序・選択を復元する。保存形式はv7を維持する
- キャンセルとEscでは一括入力の下書きを破棄して元のボタンへフォーカスを戻し、追加・保存・履歴記録は行わない。IME変換中のEscでは閉じない。次に開く際は空欄から開始する
- 既存文書の置換、英文と訳文のペア入力、CSV等のファイル取込は対象外

## formモード（活用表示）

- Normalで `f` を押すと、選択中の通常・疑似トークンまたは基礎下線の上を一時的にハイライトしてFORMモードに入る。基礎下線は含む単語数にかかわらず1つのformを持つ。角括弧・空の文・分割していない複合下線では開始しない
- D分割ではformの入力箇所も左右に分かれ、選択した側だけを編集・削除する。単語・疑似トークン・下線のD分割に共通する。T分割では分割元に1つだけformを持ち、左右どちらからでもその共通formを編集する。基礎下線を元にしたTも同様
- D分割時は既存のformを左側に引き継ぎ、右側は空にする。D解除時は左側を分割元に戻し、右側は削除する。Tの作成・解除ではformを維持する。これらの変更は構造編集と同じUndo/Redoにまとめる
- `b` →「原形」、`c` →「現在形」、`p` →「過去形」、`pp` →「p.p.」、`ing` →「ing」。対象領域の上中央に注釈色で表示し、日本語は12px、`p.p.`・`ing`は14px（行の高さは共通で18px）とする。幅計測と折り返しにも同じサイズを使う。Dは各半分の中央、Tは分割元全体の中央に置く
- 基礎下線に覆われていても、内部の通常・疑似トークンが持つformを各語の上に表示する。内部のいずれかの語にformがあれば、下線自身のformをその1段上（18px）に置く。内部にformがなければ従来の高さとし、下線自身にformがなければ上段の余白を設けない。基礎下線を元にしたTも同様で、Dは両側とも上段に置く
- 内部判定には実際の所属範囲を使い、非連続下線の隙間の語は含めない。折り返した別の行に内部の語のformがある場合も下線自身は上段に置く。語を先に配置し、同じ幅でも下線のformが上になるようにする
- 折り返し・非連続下線でも下線のformは重複表示せず、最後の表示区間に置く。未分割の基礎下線の編集中はカーソルのある区間にプレビューする。重なる対象は表示段を分ける。内部の語のformも幅計測に含め、編集・確定・削除に応じて高さと余白を再計算する
- 入力中はプレビューだけを更新し、`Enter` で確定してNormalに戻る。`p` / `pp` の区別にタイムアウトは使わない。`i` / `in` は入力途中の文字を表示し、Enterでも確定・終了しない
- 現在のバッファへの追加が有効なコードの接頭辞なら継続し、そうでなければ入力したキーを新しいコードの先頭として試す。無効なキーは無視する。`Backspace` はバッファ末尾を削る。バッファが空なら開始前の表示を維持し、Enterは無変更で終了する
- `Esc`（`Ctrl+[`も同じ）は有効な活用コードを確定してNormalに戻る。未入力や `i` / `in` の入力途中なら元の活用表示を維持して終了する。`x` は入力途中でも活用表示を即座に削除してNormalに戻る。クリックで別のスロット・組に移る場合や、別の編集開始・文書復元では未確定入力を破棄する
- `h/j/k/l`・矢印キー・`0/$`・`Ctrl+n/p` はEscと同じ規則でformを確定し、Normalに戻ってから通常の移動を1回行う。移動先がない端でもformは終了する。Dの左右や基礎下線など、移動前の対象にformを保存する
- 組内移動のキーリピートとShiftで入力する `$` は通常どおり扱う。`Ctrl+n/p` のキーリピートは終了・移動ともに抑止する。組の入れ替え `Alt+j/k`・通常の標識入力・Undo/RedoはFORM中には実行しない。formコードのキーリピート・修飾キー付き入力・IME変換中の入力も処理しない。Ctrl+rによるページ再読込は抑止する
- formの確定は1回のUndo履歴にし、その後の移動による下線の再分類などは既存の移動処理の履歴規則に従う。構造の変わらない移動は履歴を増やさない
- 確定・削除をそれぞれ1回のUndo/Redoとして記録し、自動保存する。未確定バッファは保存データに含めない。破棄・同値確定・空表示の削除では履歴を増やさない
- 通常・疑似トークンとGroupにキー入力と独立した任意属性 `form?: FormId` を保持し、空は属性なしとする。Dの左右はSlotSplitの `leftForm` / `rightForm` に保存する。未知の値・角括弧への活用設定・Tへの左右form設定は不正な保存データとして拒否する。旧v5/v6データと入力略号を保存値にしたデータは読み込まない
- 基礎下線の内部編集で一時的に複合下線へ変わってもformを保持し、基礎下線に戻れば再表示する。下線を削除するとその下線のformも削除する
- 英文変更でトークンを再生成すると活用表示もリセットする。同じ英文の再確定や疑似トークンの文字列編集では保持する。活用表示はスロットの標識や下線分類に影響しない

## 下線の呼び分け

- 直接含むスロットの内容を基準に、下線を「基礎下線」と「複合下線」に呼び分ける。初期の種類は作成時に判定し、その後も現在の内容とカーソル位置で再判定する
- 基礎下線は、直接含むスロットがすべて空の単語・疑似トークンのスロットまたはD分割の左右スロットであるものに限定する。「空」は標識が未設定（`marker` が省略または `undefined`）かつT/D分割の親でないことを指す
- 標識付きの基礎スロット、T化の左右スロット、または Group Slot を1つでも直接含む場合は複合下線とする。T化の左右や Group Slot 自身の標識が空でも、基礎下線には該当しない
- 判定対象は再帰展開後の基礎スロットではなく、現在直接参照しているスロットとする

- `Group.kind` に基礎下線は `basic`、複合下線は `composite` を保存する。上記の基礎下線の構成条件を満たす複合下線は、カーソルが内部にないときに基礎下線へ戻す。T化の左右を含む下線は標識削除・退出・再読み込みでも複合下線を維持する。作成経緯や過去の変換操作には依存しない
- 下線自身のスロットの標識は分類条件に含めない。カーソル位置の下線の種類をフッターに表示し、各下線のツールチップにも表示する

### 両方を含む初期例

英文: `She gave the student a careful explanation.`

| 対象 | 種類 | 直接含む内容 |
| --- | --- | --- |
| the student | 基礎下線 (`basic`) | 空の基礎スロット `the` と `student` |
| a careful explanation. | 基礎下線 (`basic`) | 空の基礎スロット `a`、`careful`、`explanation.` |
| gave the student a careful explanation. | 複合下線 (`composite`) | 標識 V のある `gave` の基礎スロットと、上記2つの Group Slot |

`She` の基礎スロットに S、2つの基礎下線自身のスロットにそれぞれ O1・O2 を設定する。例は `src/example.ts` で通常の作成関数を使って生成する。the student の region は [2,4)、a careful explanation. は [4,7) であり、半開区間なので衝突しない。両方の基礎下線は Y=0、複合下線は Y=1 に配置する。

## 基礎下線の内部編集

- Normal・個別選択・矢印の終点選択モードで基礎下線のスロットから `k` / `↑` を押すと、同じ ID・参照・標識を保って複合下線に変換し、再配置後のその下線から X を保って1つ上の有効なスロットへ移動する。通常の複合下線や単語スロットでの上移動は従来どおりとする
- 通常作成したものも含め、すべての複合下線を再判定の対象とする。内部編集中に条件を満たした場合は、内部を出るまで複合下線を維持する
- カーソルの Y が対象下線の Y より小さく、論理 X の基礎スロットがその下線に含まれている間を「内部」とする。非連続下線の隙間、下線自身、親下線は内部に含めない。内部での移動や、境界で動かなかった操作では変換を維持する
- 左右・上下・`0` / `$`・クリックなどで内部を出た際、直接含むスロットがすべて空の単語・疑似トークン・D分割左右で、分割の親を含まなければ基礎下線へ戻す。内部に標識を残した場合や T化の左右・Group Slot を直接含む場合は複合下線を維持する。下線自身の標識は判定に含めない
- 標識を残して退出した後でも、再び内部へ入り、標識をすべて削除して基礎下線の構成条件を満たした状態で退出すれば基礎下線へ戻る。T化の左右または Group Slot を直接含む下線は、その標識がすべて空でも複合下線を維持する
- 再配置で移動先や選択の始点にある Group Slot の段が変わった場合も、Slot ID を使って対象を維持する。重なる基礎下線を順に開いた場合も、それぞれを判定する
- 移動・クリック・文書編集・Undo / Redo の復元後に共通の再判定を行う。再配置で別の下線も復帰対象になった場合は安定するまで繰り返し、移動先と選択始点の Slot ID を保つ
- 種類の変更は自動保存と Undo / Redo の対象とし、編集に伴う再分類は同じ履歴に記録する。種類が変わらない通常の移動では履歴を追加しない。再読み込みでは内部編集を終了した状態として全複合下線を再判定し、条件を満たすものを修復する

## 個別選択と非連続グループ

- `vv` で個別選択を開始する。最初のスロットは選択済みとし、移動先で `v` またはクリックすると追加・解除する
- 範囲選択中にもう一度 `v` を押した場合は、その選択を個別選択に引き継ぐ
- `h` / `l`、`j` / `k`、矢印、`0` / `$` で移動できる。移動だけでは選択を変えない
- 基礎スロットと Group Slot を混在して選択できる。`Enter` で選択した Slot ID を直接参照するグループを作成し、`Esc` で取消する。空の選択では作成しない
- 範囲選択・個別選択から下線を作成した後は、新しい下線自身のスロットへカーソルを移す。作成と移動を1回の履歴にまとめ、Undoで作成前の文書・カーソル、Redoで作成後の文書・下線スロットへのカーソルを復元する
- 下線は再帰的に含まれる基礎スロットの区間だけに実線を描く。含まれない区間には線を描かない
- 途切れた区間の両端に同じ色の丸を付け、接続を表す。複数の途切れがある場合は接続ごとに色を変え、他のグループの接続とも区別する
- 選択やクリックの対象も実線部分とし、別グループの間にある空白が操作を遮らない
- カーソルの X/Y を含む論理 region は濃い青の背景と太い青枠で強調し、所属 Slot のほかの region は淡い紫の背景と細い紫枠で示す。折り返しで分割された同じ論理 region の表示区間もまとめて強く強調する。単語・T化の左右スロットにも同じ強調色を使う。別 region へ移動すると強い強調が移り、別 Slot へ移動すると元の Slot の強調を解除する。区間をクリックした場合は、その区間内へカーソルを移す。これは表示のみの変更であり、保存データや移動・選択の規則には影響しない
- カーソルのある下線、および範囲・個別選択に含まれる下線が直接参照するスロットを、さらに薄い青でハイライトする。単語・子下線・T化の片側に対応し、折り返しや非連続の各区間にも表示する。基礎下線に隠れた単語スロットも元の位置で表示する。スロット自身の明示的な選択は優先し、ハイライトはクリックを遮らず、保存や履歴を変更しない
- 作成・削除は Undo / Redo と自動保存に対応する。削除時は、消えるスロットに依存する下線も連鎖削除する

## 構造削除時の連鎖削除

- 下線削除とT化解除は、消失する Slot ID を起点とする共通の削除処理を使う。通常の下線削除ではその Group Slot、T化解除では左右の Slot が消失する
- 消失スロットを1つでも直接参照する下線は、ほかに残る参照先があっても下線全体を削除する。その Group Slot を参照する下線にも再帰的に削除を伝播する。参照を構成要素へ置き換えたり、一部だけ残したりしない
- 削除される下線がT化されていれば、その左右スロットと依存下線も削除する。削除対象の Group・TSplit と、それらが所有する Slot をまとめて除去し、参照切れを残さない
- 削除した下線の入力である単語・子下線・T化の左右スロットは、それ自体が削除対象にならない限り残す。同じ領域に重なっていても、消失スロットを参照していない下線は削除しない
- T化解除では元のスロットを残して左の標識を戻すため、元のスロットだけを参照する下線も残す
- 連鎖削除全体を1回のUndo / Redoにまとめる。削除後は残るカーソル対象を Slot ID で維持し、消えた場合は元の位置に対応する単語の有効な段へ補正する。T字の削除に伴う再採番も反映し、Undoでは削除前のID・標識・参照・カーソルを復元する。結果は組内のv6データとして文書のv7形式に含めて自動保存する

## Undo / Redo

- 履歴は全組共通。別の組の編集と追加・削除・入れ替えも含めて操作順に戻す
- 文書全体、編集中の組ID、カーソル位置を復元し、対象の組へスクロールする。追加・削除・入れ替えは各1回の編集とし、削除のUndoでは元の組IDと全構造を復元する

- Normal / Visual / 個別選択モードで `u` は Undo、`Ctrl+r` は Redo
- 下線・矢印の作成・削除・付け替え、内部編集に伴う種類の変更、標識の入力・削除、英文・訳文の編集が対象。データと編集時のカーソル位置を復元し、Normal モードに戻る
- 英文・訳文は入力を開始してから `Esc` または「完了」（訳文は `Enter` も可）で確定するまでを1回の編集として扱う
- 移動・選択・変更のない入力は履歴に追加しない。Undo 後に新しい変更を確定すると Redo 履歴を破棄する
- 復元結果も自動保存する。Undo / Redo 履歴はページを開いている間だけ保持する
- テキスト入力中の `u` は文字入力。`Ctrl+r` によるページ再読み込みは抑止する

## T化

- 基礎スロットは単語スロット、基礎下線のスロット、T化の左右スロットを指す。ただし基礎下線の構成対象とは区別し、T化の左右または基礎下線の Group Slot を含む下線は複合下線にする。矢印の終点位置補正など、下線分類以外でのT左右の基礎扱いは維持する
- Normal モードの `t` で単語スロットまたは連続した基礎下線をT化する。複合下線、非連続下線、T化済みの親、T化で生成した左右スロットでは無変更。キーリピートでは作成しない
- 横線は通常の下線と同じ位置・色・太さにする。中央から下へスロット高さ（28px）分の縦線を描き、左右に独立したスロットを置く
- 折り返しは通常どおり行い、最後の表示区間にだけ縦線と左右スロットを描く。前の区間は横線と接続点を残す。左右を参照する下線も、最後の区間の該当する半分を描画範囲とする
- 作成時は元の標識を左へ移し、右は空にする。カーソルは左へ移動する。左右それぞれで標識入力、範囲選択、個別選択、下線作成を行える
- 元のスロットIDは保持し、既存の下線はその全体を参照し続ける。T化の親は内部に左右スロットを持つため、標識が空でも「空のスロット」として再分類しない
- `h` / `l` は左・右・隣接スロットを順に移動する。クリックでも左右を個別に選べる。T化中の基礎下線では `k` / `↑` による内部編集は行わない
- 左右スロット上の `X` でT化を解除し、左の標識を元のスロットに戻す。右の標識は破棄する。左右を参照する下線とその依存下線、それらが持つT化データも連鎖削除する。元のスロットへの参照だけを持つ下線は保持する
- 作成・解除と連鎖削除はそれぞれ1回の編集としてUndo / Redo・自動保存に対応する。Undoでは左右のID、標識、依存下線とカーソルを復元する

## D分割（線なしの2分割）

- Normalモードの小文字 `d` で単語スロット、または論理領域が連続した基礎下線・複合下線のスロットを左右に分割する。複数要素の下線は現在カーソルの要素の直後（末尾なら直前）を境界とし、左右非対称の分割を許可する。単語スロットは従来どおり二等分する。非連続下線、T・D分割済みの親と左右スロットは対象外。キーリピート・無効操作では変更も履歴追加も行わない
- 同じ高さの左右2枠を表示する。T分割の横線・中央の縦線は追加しない。既存の下線と接続点は残す
- 行をまたぐ下線も、論理的に連続していれば分割できる。作成時の左右比は論理範囲の復元用に保存し、表示上の左右枠はT同様に最後の表示区間だけへ置く。表示幅は標識・formの必要幅によって論理比と異なってもよい。幅変更では論理範囲を変えない
- 標識と出る矢印を左へ移し、右は空にしてカーソルを左へ移す。元スロットIDと全体を参照する下線・入る矢印は保持する
- 左右の標識入力・選択・下線作成・矢印・移動はTと共通。ただしDの左右は空なら基礎下線を構成できる。分割中の基礎下線は上移動による内部編集を行わない。複合下線は通常の上移動ができる
- 左右上の `X` で解除し、左の標識を親へ戻す。右の標識は破棄し、消える左右に依存する下線・分割・矢印を連鎖削除する。元スロットのみを参照する構造は残す
- 作成・解除はそれぞれ1回のUndo / Redo、自動保存に対応する。`ad`・`sad` などの標識入力中の `d` は続きの入力として優先する。テキスト入力・選択・矢印モードではD分割しない
- 保存は組内v6・文書v7とする。分割共通型 `SlotSplit` に任意の `kind: 't' | 'd'` を持ち、省略はTとして扱う。Dは `kind: 'd'` と、非対称の場合は左側の比率 `ratio` を保存する。`ratio` の省略は1:1として扱う。未知の種類、範囲外の比率、非連続領域、分割の左右への再分割は不正とする。Tの対象条件は変更しない

## 矢印

### 従来の矢印（有向）

- Normalモードの `r` で現在のスロットを始点として終点選択（ARROWモード）を開始する。始点標識は `a` / `ad` / `ado` / `sad` に限定し、空や他の標識では案内を表示して開始しない
- `h/l/j/k`・矢印キー・`0/$` またはクリックで終点を選び、`Enter` で確定、`Esc` で取消。始点自身は終点にできない。選択中は始点を黄色で強調し、標識入力・構造編集を禁止する。ただし `k` / `↑` では通常時と同じく基礎下線の内部へ入り、内部の単語も終点に選べる。T・D分割中の基礎下線には入らない
- 終点選択中も内部を出ると通常の条件で基礎下線へ戻す。矢印の接続は復帰を妨げず、始点・終点のSlot IDを維持する。内部で `Esc` を押しても下線は強制的に閉じず、その後の退出時に再判定する。種類変更の履歴・自動保存は通常の移動と同じ扱いとする
- 各始点からは最大1本。同じ終点への複数の始点は1本の横線を共有する。既存矢印の始点で `r` は付け替え、取消では元の矢印を保持。`Shift+r` は現在の始点の矢印だけを削除する。キーリピートでは作成・削除しない
- 横線の配置を先に決め、各スロットの最後の表示区間の中央から垂線を接続する。始点は下辺から、終点は空なら上辺より8px上（`スロットの論理Y × 38 − 8px`）・標識付きなら下辺へ接続し、終点側に上向きの矢じりを描く。ただしT字の左右スロットへの終点は、空でも下辺（`スロットの論理Y × 38 + 28px`）に接続する。T左右以外の空の終点は基礎下線の有無によらず同じ長さとし、含まれる基礎下線がある場合は交差して約3px上へ突き出す。D左右はこの通常規則に従う。専用接続座標のある記号はその座標を優先する。表示上のピクセル座標は衝突判定に使わず、最後の論理 region の中点を用いる
- 同じスロットが矢印の終点と始点を兼ねる場合、終点側は中央に保ち、出る矢印の始点だけを行き先方向へ6pxずらす。狭いスロットでは表示区間幅の1/4、同じ行の近い終点では接続点間距離の1/2までに制限する。折り返しでは後の行を右方向、前の行を左方向とし、同じ行・同じ中央位置の場合は右へずらす
- 横線の左右端もずらした接続位置から求め、始点だけが決める開いた端はその分短くする。他の始点や終点が元の端に残る場合はその接続を保持する。中間の始点をずらしても横線の外端は変えない。この補正は描画専用で、論理区間・衝突判定・段配置・保存データは変更しない
- 横線は下線と同じ色・太さ。折り返しでは行ごとに分割し、同色の接続点でつなぐ。縦線・矢じりは接続する表示行にだけ描き、描画はクリックを遮らない
- 矢印横線の高さは、各表示行のY=0スロット上辺を原点として `論理Y × 38 + 14px` とし、同じ段の高さ28pxのスロット中央に揃える。その段に実際のスロットがなくても同じ高さを使う。縦線は既存のスロット側接続点から横線まで延ばし、矢じりの先端はスロット側に保つ。折り返しの接続点も横線に追従する
- 描画区間は `logicalY` とピクセルの `y` を分けて保持する。表示行の最大段数は `logicalY` から求め、SVGは最下段スロットの下辺（`最大論理Y × 38 + 28px`）まで確保する。ピクセル位置からの段数逆算は行わない
- 作成・付け替え・削除はそれぞれ1回のUndo / Redoにまとめ、自動保存する。`Ctrl+r` はRedoであり、矢印作成やページ再読込には使わない。Undo / Redoでは終点選択を終了する
- 始点の標識を対象外へ変更・削除すると、その編集の確定時に矢印も同じ履歴で削除する。`sad` などの入力途中では削除せず、標識入力中の保存を確定まで待つ
- スロットの連鎖削除では消える始点・終点に接続した矢印を除去し、共有横線を再計算する。矢印の削除から下線の削除は伝播しない
- T化時は標識と出る矢印を左スロットへ移す。親スロットを終点とする矢印は維持する。解除時は消える左右スロットに接続した矢印を削除する
- 英文変更でスロットを再生成すると矢印もクリアする。矢印の追加・削除・再分類で段が変わっても、残るカーソルと選択始点はSlot IDで維持する

### 同格の矢印（双方向）

- Normalモードの `n` / `s` / `o` / `nc` / `nC` のスロットで `r` を押すと、同格の相手選択を開始する。相手は同じ標識集合または空スロット。自己接続は禁止し、対象外では案内を表示して選び直す。空スロットからは接続済みでも開始できない
- 移動・クリック・Enter確定・Esc取消・基礎下線への出入り・編集禁止・キーリピート抑止は従来の矢印と共通。開始スロットを黄色で強調し、案内に「同格」と相手の条件を表示する
- 同格は2スロットを対等に結ぶ1本で、各スロットは同格1本まで参加できる。従来の矢印の入射と併存できる。確定時には両端に接続していた同格を外して新しい1本へ置換し、取消では既存接続を維持する。同じペアへの再接続は変更なしとする
- `Shift+r` は同格のどちらの端でも削除でき、空側でも有効。従来の矢印は始点からだけ削除し、同格の削除では従来の入射矢印を残す
- 同格は束ねず、1接続ずつ共通段配置に参加する。両端の最大Y+1から開始し、短い順・下線優先・同種の保存順・衝突時の下段送りは従来どおり。矢印同士の衝突区間は接続中心間の両端開区間、下線との衝突区間は両端スロットの全regionの外接半開区間とする。配置キーは従来の矢印と区別する
- 横線は `論理Y × 38 + 3px`、同じ段の高さ28pxのスロット上辺から3px下に描く。両端とも最後の表示regionの中央へ垂線をつなぎ、標識付きは下辺、空は上辺より8px上（`スロットの論理Y × 38 − 8px`）に接続する。ただしT字の左右スロットは、保存上の始点・終点によらず空でも下辺（`スロットの論理Y × 38 + 28px`）に接続する。T左右以外の空の端点は基礎下線の有無によらず同じ長さとし、含まれる基礎下線がある場合は交差して約3px上へ突き出す。両端が空の場合も双方に適用する。D左右はこの通常規則に従う。矢じりは付けない。従来の矢印の終点と重なる端は、相手側へ向かって6pxずらす。狭いスロット、同じ行の近い相手、折り返し、同じ中央位置では従来の矢印の入出力併存時と同じ制限・方向決定を使う
- 折り返しと同色接続点、線色・太さ、クリック非遮断は従来どおり。同格を示す `+` は最後の表示regionの横線の中点下に1つだけ描く。文字は14px、中央揃え、横線からベースラインまで18px、線と同色・縁取りなしとする。始点・終点の保存順によりラベル位置は変えない
- どちらかの標識が対象外へ変わった場合、標識編集確定時に同格を同じ履歴で削除する。空への変更は削除せず、空同士の接続も有効とする。開始キーの制限と保存状態の妥当性は区別する
- T / D 分割では、どちら側の接続でも分割後の左スロットへ移す。分割解除や構造の連鎖削除では消えるスロットへの接続を削除する。従来の矢印の親への入射は維持する
- 作成・双方の既存接続の置換・削除はそれぞれ1回のUndo / Redoと自動保存にまとめる。英文再生成時は同格もクリアし、配置変更時はカーソルをSlot IDで維持する

## 文書の保存データ v7

```ts
type Entry = { id: string; document: SavedState };
type EntryDocument = { version: 7; entries: Entry[] };
```

- 論理形式はv7とする。組は1件以上、組IDは空でない一意の文字列とする。配列順が表示順であり、組番号は保存しない
- 永続化にはWeb Worker内のIndexedDB（`kiri-kyo-editor`）を使う。`entries`ストアで組IDごとの文を保持し、`metadata`ストアで並び順と保存要求の適用済み番号を保持する
- 各組の内容には下記のv6型を使用する。Slot・Group・TSplit・Arrowの参照は、その組内だけで検証し、別の組への参照を拒否する
- localStorage・IndexedDB・復旧ジャーナルの旧v5/v6形式は移行せず拒否する。読込失敗・不正データはエラー表示して保持し、初期サンプルで上書きしない。エラー表示から確認を経て保存データを破棄でき、破棄後は初期サンプルで再初期化する。保存データが存在しない場合だけ初期サンプルを使用する
- 操作停止から500ms後に、編集中の組の変更だけを非同期保存する。クリック・キーボードで別の組へ移る場合は、移動前の編集を既存規則で確定して保存キューへ渡し、保存完了を待たずに移動する
- 追加・一括追加・削除・並べ替え・Undo/Redoは、変更した文と並び順だけを同じトランザクションで保存する。編集中の組ID・カーソル・履歴は永続化しない
- 保存キューは要求を順番に処理し、同じ文の未送信更新は最新の保存可能な内容へまとめる。要求IDと変更世代を使い、古い完了通知で新しい編集を保存済みにしない。失敗した要求は同じIDで次の保存機会または再試行ボタンから再送する
- 対象の組で標識入力途中・訳文のIME変換中は通常保存を抑制し、確定・変換終了後に改めて500ms待つ。他の組の未確定状態では保存を止めない。途中保存でUndo履歴を追加しない
- 再読み込み・ページ離脱時だけ、未保存の文・削除・並び順変更と要求IDをlocalStorageの `kiri-kyo-editor:recovery:` 接頭辞付きセッション別キーへ同期退避する。次回起動では適用済み要求を除外して復元し、成功後に退避を削除する。離脱イベントが発火しない強制終了は保証しない
- 保存済みはトランザクション完了後に表示し、保存中・未保存・失敗を区別する。WorkerやIndexedDBの失敗時に通常保存を同期方式へ戻さない

### 組内の保存データ v6

```ts
type CustomMarker = { kind: 'custom'; text: string };
type Marker = MarkerId | CustomMarker;
type Slot = { id: string; marker?: Marker };
type SlotSplit = { slotId: string; leftSlotId: string; rightSlotId: string; kind?: 't' | 'd'; ratio?: number; leftForm?: FormId; rightForm?: FormId };
type TSplit = SlotSplit; // 旧型名との互換用
type Arrow = { sourceSlotId: string; targetSlotId: string; kind?: 'apposition' };
type FormId = 'form.base' | 'form.present' | 'form.past' | 'form.pastParticiple' | 'form.ing';
type Token =
  | { id: string; text: string; slotId: string; kind?: 'real'; form?: FormId }
  | { id: string; text: string; slotId: string; kind: 'pseudo'; form?: FormId }
  | { id: string; text: '['; slotId: string; kind: 'bracket-open' }
  | { id: string; text: ']'; kind: 'bracket-close' };
type GroupKind = 'basic' | 'composite';
type Group = {
  id: string; slotId: string; slots: string[]; kind: GroupKind; form?: FormId;
};

type SavedState = {
  version: 6;
  arrows: Arrow[];
  splits: SlotSplit[];
  tokens: Token[];
  slots: Slot[];
  groups: Group[];
  translation: string;
};
```

- Token 配列を英文の正本とし、表示時は単一スペースで連結する
- Token と Group はそれぞれ UUID 付きの Slot を持つ。TSplit は親の Slot ID と、新しく所有する左右2つの Slot ID を保持する
- `marker` は `marker.subject` などのキー入力と独立した `MarkerId`、または `{ kind: 'custom', text }` の自由標識を保存する。省略時は空のスロットとする。自由標識の `text` は前後空白のない空でない文字列に限定する
- Group の `slots` は、構成要素となる Token、Group、またはT化の左右の Slot ID を左から右の順で保持する
- 下線範囲・矢印の終点ごとの束・描画段は Slot 参照から導出し、保存しない
- Arrowのkind省略は従来の有向矢印、`apposition`は同格とする。保存バージョンは維持し、識別子のない旧データも従来どおり読み込む。同格のsourceSlotId / targetSlotIdは保存上の参照名であり、方向を表さない
- 矢印配列の欠落、未知のkind、自己接続、参照切れは不正とする。従来の矢印は始点の重複・対象外の始点標識を拒否し、同格は両端の重複参加・空でも許可標識でもない端点を拒否する。両種類の参加制限は別々に判定する
- 組内のデータ型はv6に限定する。文書全体のv7と単一組v6だけを読み込む
- Slot の重複所有・欠落、種類の欠落・未知の種類、基礎下線から Group Slot またはT化の左右への参照、循環参照を不正とする。T化の親は単語または連続した基礎下線に限定し、親の標識・左右IDの重複・T化の重複・左右を経由する循環も不正とする
- T化の左右を含む不正な基礎下線は、Tの `kind` が省略・`t` のどちらでも読み込みを拒否する。組内v6・文書v7を使用し、互換変換や保存領域全体の自動削除は行わない

## 下線・矢印の共通段配置

- Token Slot を基礎スロットの一種とし、すべて Y=0 に置く。基礎下線で覆われても Token・Slot・標識・参照は保存データに保持する
- 単語境界とT字の論理的な二分位置で最小区画を作り、左から整数の境界番号を振る。最小区画は [i,i+1)、Slot の region はそれらを参照から再帰展開した半開区間とする。下線も同じ半開区間を占有し、隣接する下線やT字の左右は境界だけでは衝突しない
- 二分位置は再採番前の単語空間で計算し、全分割境界が確定してから整数に再採番する。他のT字の追加で既存の二分位置は変えない。追加・解除・連鎖削除後も欠番は残さず、Slot ID・参照・カーソル・選択始点を維持する。再採番自体は履歴に追加しない
- 論理Xと単語配列番号は分離する。各最小区画は所属する単語番号と Slot ID を持つ。region と座標は保存せず、組内v6を再読込するたびにこの規則で再計算する
- 下線と終点ごとの矢印束を共通の待ち集合にする。直接参照先・全接続先が配置済みの候補から、下線は全regionの最右境界−最左境界、矢印は最大接続X−最小接続Xを長さとして、最短のものを毎回選ぶ。非連続下線の長さも全体幅とする。同長は下線優先、同種内は保存順（矢印束は最初の所属矢印の順）とする
- 基礎下線は直接参照する Slot の最大 Y（単語のみなら0）、複合下線は最大 Y+1、矢印は全始点・終点の最大 Y+1を開始段とする。区間がその段の配置済み下線・矢印の横線と衝突する場合は Y を増やし、最初の空く段を選ぶ。単語スロットや縦線との交差はこの判定に含めない
- Group 自身の Slot はその下線と同じ Y を持つ。T化の左右も親と同じ Y を持つ。配置結果を描画・クリック・キーボード移動で共有する
- 下線は自身の Group Slot 領域の直上に描く。Y=0 の基礎下線のスロット領域は単語スロットと同じ高さとする。非連続区間の接続点も線上に置く
- 矢印の論理接続Xは、各スロットの最後の論理region [x,y) の中点 (x+y)/2 とする。非連続部分全体の中央や最後の表示区間のピクセル座標は用いない。中点は整数または .5 となる
- 矢印同士の衝突区間は全始点・終点の論理接続Xの最小値〜最大値。始点が決める端は開、終点が決める端は閉とし、同じ端を両者が決める場合は閉とする。始点region [2,4)、終点region [5,7) なら (3,6] となる
- 上記の開閉端は従来の有向矢印に適用する。同格は両端閉区間で、接続の保存順によらず同じ衝突判定になる
- 矢印とスロット（下線）の衝突判定では、矢印の当たり判定を全始点・終点スロットを完全に含む最小の半開区間に切り替える。全接続スロットの全regionについて最小start〜最大endを取り、非連続部分の隙間も含める。上記の例では [2,7) となる。矢印が先に配置済みでも下線が先でも同じ判定を行う。矢印同士の判定・短い順の長さ・描画接続位置は変更しない
- 端点だけの接触は双方がその点を含む場合に限り衝突する。下線は左端を含み右端を含まない。両端が同一点の閉区間は点として判定し、開端を含む長さゼロの区間は空とする。非連続下線の隙間は占有せず、矢印の横線は全区間を占有する
- 矢印自身はスロットを持たず、矢印だけの段は移動時に飛ばす。幅変更は表示だけを変え、論理配置は維持する
- 配置は決定的だが、全体の最小段数は保証しない

## 表示位置と操作対象

- 同じ論理 X/Y に Group Slot があれば、それを表示・移動・選択・標識編集の対象とする。Y=0 で Group Slot がなければ Token Slot を対象とする
- Y=0 の基礎下線が含む列では、元の単語スロットのボタン・標識・強調表示を隠し、高さを保つスペーサーに置き換える。単語の文字列は表示し続ける。非連続下線の隙間は単語スロットを操作できる
- 幅計算には表示される標識のみを使い、隠れた単語スロットの標識は含めない
- 下線自身の標識は単語幅とは別に測定する。下線の幅は内部の表示幅（単語間隔・子下線の表示幅を含む）と標識幅の大きい方とし、標識が広い場合は不足分を左右に均等に追加する。内部を中央に置き、下線自身の標識を理由に末尾などの単語幅や内部の単語間隔を広げない
- 拡張分は下線の外側の余白として確保し、隣接する独立した下線・単語とは10pxの間隔を保つ。入れ子では子下線の表示幅を親の内部幅に含める。外側の余白は後続の単語位置・折り返しに反映するが、論理範囲・段配置・保存データは変えない
- 折り返し・非連続下線では、標識を表示する最後の表示区間だけに標識幅を確保する。入力中は入力文字を表示する区間に確保し、確定・取消・削除・Undo/Redo・画面幅変更では元の測定幅から再計算する。拡張した描画範囲が近づいても、非連続の論理範囲は連結しない
- 下線のT分割は左右の標識の大きい方の幅を両側に確保し、最後の表示区間を左右等幅にする。D分割は左右それぞれの表示標識、自由標識の入力中文字列、formのうち最大の必要幅を個別に確保する。D全体の表示幅は元の単語・下線幅と左右の必要幅の和の大きい方とし、元領域と中央を揃える。単語スロットのD分割では単語をD全体幅の中央に表示し、その必要幅を単語自体の幅や単語間隔へ転嫁しない
- 標識だけで表示可能幅を超える場合は単語列をさらに広げたり標識を縮小・切断したりせず、図の横スクロールで表示する
- 範囲選択・個別選択は基礎下線の Group Slot を直接参照し、内部の Token Slot に置き換えない。`k` / `↑` で基礎下線を複合下線へ一時変換すると、内部へ移動できる
- `X` はT化の左右スロット上ではT化解除を優先する。それ以外では現在座標の下線を優先して削除する。単語スロット上では従来どおり現在位置を含む最新の下線を削除する。削除後は再配置し、元の単語スロットまたは別の基礎下線を操作対象に戻す
- `0` / `$` は Y=0 では文の先頭・末尾、それ以外では現在段の先頭・末尾へ移動する
- 配置と操作対象は保存済みの組内v6データにも適用し、Undo / Redo と再読み込みで保持した内部スロットを復元する

## 左右移動

- 論理 X は最小区画の整数境界を使う。T化の追加・解除では区画を再採番する。画面幅変更では再採番せず、論理位置・衝突区間・段配置を維持し、表示だけを最後の区間に合わせる

- region は、入れ子を再帰展開した基礎 Slot の論理 X 上の連続区間（始端を含み終端を含まない）とする。1つの Slot は複数の region を持てる。現在の X を含む region を移動の単位とし、同じ Slot の別 region はその移動境界に含めない
- `h` / `←` は、region の左端直前の最小区間の論理 X へ移動する
- `l` / `→` は、region の右端（そのregionには含まれない）から始まる最小区画の論理 X へ移動する
- 移動先の X を固定し、現在の Y でカーソルが有効ならそのまま着地する。無効なら有効になるまで `Y -= 1` を繰り返す。Group がなければ基礎 Slot 自身（Y=0）へ移動する
- Y=0 の基礎下線でも現在の region の端を使い、非連続下線の各 region とその間の基礎 Slot を順に移動できる。Group Slot に覆われていない基礎 Slot 上でも直前・直後の最小区間へ移動する。移動先が文の範囲外になる場合と空文では、X・Y ともに現在位置を維持する
- 表示の折り返しでは論理 region を分割しない。画面幅にかかわらず同じ論理的な端を使う。同じ段の遠くの Slot を優先せず、移動履歴も使用しない
- Normal・範囲選択・個別選択で共通の移動規則を適用する
- region の移動後も、下線・標識・範囲選択・個別選択の操作単位は Slot とする。同じ Slot の別 region から操作しても同じ Slot 全体を対象とする

## 上下移動

- X を固定し、移動方向の近い段から、その X を含む region を探して移動する。Slot の同一性や、横に離れた別 region を移動先の判断に使わない
- 下方向に該当する region がなければ X・Y ともに維持する。上方向は Y=0 まで探す。通常の Y=0 では上へ移動しない
- 基礎下線の `k` / `↑` による内部編集は従来どおり維持し、変換後も X を保って内部へ入る。T化中の例外と、上下移動を使えるモードは変更しない

## 標識の入力

- Normal モードで現在の Token Slot / Group Slot / T化の左右スロットに標識を1個入力する。既存の標識は置換し、小文字 `x` で標識だけを削除する。大文字 `X` は下線削除・T化解除と依存下線の連鎖削除
- Normal モードの `/` は、現在の編集可能なスロット内に既存表示を初期値とするネイティブ入力欄を開く。入力中は保存データを変えずに表示幅・折り返しをプレビューし、Enterで確定、Esc・`Ctrl+[`・blurで取消する。IME変換中の確定・取消キー、修飾キー、キーリピート、通常コマンド、組間操作は入力として保護する。Borderモードの `/` は従来どおり疑似トークン作成とする
- 自由入力の確定時は前後空白を除去し、空なら標識を削除する。既存の表示ラベルと完全一致する文字列は対応する `MarkerId` に変換し、それ以外は自由標識として保存する。自由標識は非空標識として下線分類・幅・矢印接続位置へ反映するが、矢印・同格の意味条件は満たさない。確定時に不適格となった既存矢印を同じ編集内で削除する
- 自由入力の開始から確定までをUndo / Redo 1回にまとめ、同値確定・取消では履歴を追加しない。丸括弧・山括弧など編集不可スロットと空文では `/` を消費するだけで入力を開始しない
- `src/inputIds.ts` の意味IDと表示ラベル、`src/inputConfig.ts` の入力シーケンス定義を分離する。入力判定・操作ガイドは入力設定を使い、保存・表示・ドメイン判定は意味IDを使う
- `+` を入力すると標識 `+` を設定する（`+ → +`）。
- 対応は `s → S`、`s' → S'`、`V → V`、`1〜5 → (1)〜(5)`、`-3 → (-3)`、`-4 → -(4)`、`-5 → -(5)`、`a → a`、`ad → ad`、`ac → aC`、`nc → nC`、`nC → nC`、`o → O`、`o1 → O1`、`o2 → O2`、`n → n`、`aux → aux`、`pre → 前`、`con → 接`、`ado → 副詞的目的格`、`sad → 文ad`、`nad → 誘導副詞`。大文字・小文字を区別する
- 一致した標識は即時表示し、長い候補があれば続きの入力を待つ。タイムアウトはない。`a` の後に `d` なら `ad` に更新し、`ad` の後に `o` なら `副詞的目的格` に更新する。`s` → `sa` → `sad` は `S` を表示した後に `文ad`、`a` → `au` → `aux` は `a` を表示した後に `aux` となる。`aux` の途中の `u` と `x` は標識入力として扱う。`n` → `na` → `nad` は `n` を保持した後に `誘導副詞` へ更新し、最後の `d` でD分割しない。一続きの入力を Undo 1回で入力前に戻す
- 続きとして一致しないキーは入力を区切り、そのキーから新しい標識または通常の操作を処理する。移動・クリック・モード変更・フォーカス離脱でも区切る
- `Esc` は有効な標識を確定して未完成の接頭辞を破棄する。`-`、`p`、`pr` のみでは既存の標識を変更しない。`n` は即時表示し、続く `c` または `C` で `nC` に更新する。`s` は `S` を即時表示し、続く `'` で `S'` に更新する。Shift 単独では区切らず、IME 変換中や修飾キー付き操作は標識入力に使わない
- 標識はスロット内に中央表示する。非連続・折り返しで分割された下線では論理的に最後の区間だけに表示する。どの区間からも同じ標識を編集できる
- 続きの入力を待っている間は、上部の仮入力表示ではなく現在のスロット内に入力バッファを灰色で表示する。非連続・折り返し下線ではカーソルに対応する表示区間に表示し、T化の左右スロットにも適用する。確定・取消時は通常の標識表示に戻す。仮入力も幅の計測に含め、保存データと履歴の扱いは変えない
- 表示されるトークンの標識と、そのトークンを最終列とする下線の標識の幅を列幅の下限に反映し、折り返し計算に含める
- 入力・削除は自動保存と Undo / Redo に対応する。未完成バッファは保存しない。同じ標識の再入力や空スロットの削除では履歴を増やさない

## 疑似トークン

- Border モードの `/` でネイティブ入力欄を開き、確定時に前後空白を除去して連続する空白文字ごとに分割する。各要素は独立したIDとスロットを持つ疑似トークンとして入力順に生成する。空入力・空白だけの入力は取り消す
- 複数生成は一括で構造検証し、1回の Undo / Redo・自動保存とする。確定後は生成した全トークンの右境目で Border モードを継続する。入力中は単一の仮トークンとして幅と折り返しをプレビューする
- Normal モードで疑似トークン自身またはそのT/D左右スロットから `i` を押すと、1トークンの文字列として再編集する。再編集では空白分割せず、空文字で確定した場合のみ疑似トークンを削除する
- Border モードの Backspace は左隣、Delete は右隣が括弧または疑似トークンの場合に削除する。通常トークンは削除しない。疑似トークンの削除は所有スロットを起点に、T/D分割、依存する下線・上位構造・矢印も連鎖削除し、削除後も Border モードを維持する
- Normal モードの `X` は下線削除・T/D分割解除にのみ使用し、疑似トークン自体は削除しない

## デバッグ用JSON

- ページ下部の折りたたみ欄に「表示状態」と「保存状態」の読み取り専用JSONを表示する。全文JSONは欄を開くか、更新・コピーを明示操作した場合だけ生成する。通常の保存経路では全文の組み立て・JSON生成・textarea更新を行わない
- 表示状態にはv7文書全体・編集中の組ID・各組のモード、論理カーソルと対象 Slot ID、選択、標識入力バッファ、英文入力中の下書き、現在の文書、段配置、列幅、折り返しと描画区間を含める。Map はキー付きオブジェクト、Set は配列として内容を出力する
- 保存状態には保存先、Worker経由でIndexedDBから読み取った保存完了済み文書、保存・読み取りエラーを含める。画面内の文書から保存状態を推測しない。取得操作で手元の文書を書き戻さない
- 「両方のJSONをコピー」では遅延表示の内容ではなく最新状態を取得し、`display` と `saved` を含むJSONをクリップボードへコピーする。コピー自体で文書の保存や編集確定は行わない。失敗した場合は手動コピーを案内する
- デバッグ欄の操作でカーソルや選択を変更しない。コピーで標識入力バッファを確定せず、JSON欄でのキー操作はエディタのショートカットとして処理しない

## テスト実行方針

- 通常は `npm test` で高速テストのみ実行する。設計上必ず実時間200ms以上かかるテストや手動ブラウザー検証は、全テストまたは遅いテストの明示要求がある場合だけ実行する
- 遅い自動テストは `tests/slow/` 配下または `*.slow.test.mjs` に分離し、明示要求時に `npm run test:all` / `npm run test:slow` を使う
- 時間境界は偽時計で検証する。仮想時間の進行は実時間の待機とは区別し、テスト群の合計所要時間や実行環境による偶発的な遅さで分類しない。恒久的な作業ルールは `AGENTS.md` に記録する

## 今回は実装しない範囲

複数標識の保持、組の分割・結合、PDF出力は次の段階で実装する。
